# 业务模糊点确认清单

> **用途**：记录当前小程序里"代码行为已定、但业务口径还没跟需求方对齐"的地方。每条都写明【现状】（代码实际怎么做的，附文件位置）和【待确认】（需要业务方拍板的问题）。
>
> **状态标记**：`[已定]` 结论明确无需再讨论 · `[待确认]` 需要业务方答复 · `[阻塞]` 影响上线或资金结算
>
> **维护约定**：新增一条时先写现状（带代码位置），再写待确认问题。已拍板的把结论写回【现状】并标记 `[已定]`，不要直接删掉，留痕便于追溯。

---

## 0. 角色对照表

| 角色 key | 界面显示名 | 是否绑定门店 | 说明 |
|---|---|---|---|
| `chef` | 门店下单人员 | 必须 | 下单人。登录页角色名写作「下单人员」 |
| `store_manager` | 店长 | 必须 | 门店负责人 |
| `purchaser` | 管理员 | 否（可跨店） | 采购/集团管理员 |
| `super_admin` | 超级管理员 | 否（可跨店） | 唯一，`admin` 账号不可删除、不可改角色 |

代码位置：`cloudfunctions/authService/index.js:12-18`

---

## 1. 厨师（下单人员）是否具有验收资格 —— `[已定]`

### 结论：**没有**

验收提交角色白名单只包含 `store_manager` / `purchaser` / `super_admin`，`chef` 不在其中。

### 现状（三处都在拦）

1. **后端硬拦截**：`cloudfunctions/createReceipt/index.js:102`
   ```js
   if (!['store_manager', 'super_admin', 'purchaser'].includes(user.role)) {
     return { code: -403, msg: '当前账号无权提交收货验收' }
   }
   ```
2. **前端不显示验收入口**：`pages/purchase-list/purchase-list.js:97`、`pages/purchase-detail/purchase-detail.js:33`
   ```js
   const canReceiveRole = currentUser.role !== 'chef'
   ```
3. **厨师看不到收货记录**：`cloudfunctions/getReceipts/index.js:34-36` 对 `chef` 直接返回空列表。

### 厨师实际权限范围

| 能力 | 是否可用 |
|---|---|
| 创建采购单 | ✅ |
| 查看自己创建的订单 | ✅（`getPurchaseOrderDetail/index.js:47` 校验 `created_by` 为本人） |
| 查看门店范围报表 | ✅（`getPurchaseOrderDetail/index.js:78` 只放行 `report_scope === 'store'`） |
| 收货验收 / 查看收货记录 | ❌ |
| 采购审核、商品/供应商/价格管理 | ❌（`pages/index/index.js:93`，仅 `purchaser` + `super_admin`） |
| 查看消息中心 | ✅ 已按门店隔离，但**不按人定向**——能看到本门店全员的消息（见第 5 条） |

### 遗留问题

- 验收页 `pages/receive-verify/` 自身**没有**角色前置校验：厨师手动拼 URL 能进入表单、填完提交才在后端被拒。体验上是"填一堆然后报错"，建议在页面 `onLoad` 加一道角色判断。
- `pages/receive-list/` 当前是**无入口死页面**：`app.json` tabBar 和首页均无收货入口，验收入口只在采购列表/详情页的「去收货」按钮上；该页 `receive-list.js:19` 仍保留 `chef` 的 `createdBy` 过滤分支，属废代码。

---

## 2. 未审批的订单可以直接验收 —— `[阻塞]`

**待确认：是否需要"审批通过才能收货"？**

`createReceipt/index.js:166` 允许收货的订单状态包含 `submitted`（已提交、**尚未审批**）：

```js
if (!['submitted', 'approved', 'report_generated', 'partial_received', 'to_receive'].includes(order.order_status)) {
```

后果：店长可以在采购单还没走完审批流程时直接验收，审批流被事实性绕过。采购审核（`approval-list`）与验收（`receive-verify`）的职责边界因此不清晰。

**待业务方拍板**：
- 方案 A：只允许 `approved` 及之后的状态收货（严格，审批是收货前置）
- 方案 B：维持现状（门店先收货、审批并行，适合供应商到货不等人）

---

## 3. "部分收货"目前做不出来 —— `[阻塞]`

**待确认：是否真的需要分批/多批次验收？**

订单状态字典里有 `partial_received`（部分收货），但它只作为"可继续收货"的输入状态存在，没有任何代码路径会把它写成结果：

- `createReceipt/index.js:212-214` 要求本次提交的明细必须覆盖**全部**订单行：
  ```js
  if (canonicalItems.length !== orderItems.length) {
    return { code: -1, msg: '验收明细不完整，请确认所有商品后重试' }
  }
  ```
- 订单已存在收货单即视为重复，禁止二次提交（`createReceipt/index.js:220-222`）。
- 实收数小于下单数时**不产生** `partial_received`，而是强制打异常标（见第 4 条）。

即当前实现是"一单一票、整单一次验收完"，`partial_received` 是空状态。若业务上确有分批复货场景（如同一采购单隔天再送一批），现有模型支撑不了。

### 补充（2026-09-11 二次排查）

**空状态的影响面比原记录更大。** 除了 `createReceipt` 的两处校验，`partial_received` 还被 3 处读作"可收货"的判定条件：

- `createReceipt/index.js:166`（事务外状态校验）
- `createReceipt/index.js:277`（事务内二次校验）
- `pages/purchase-list/purchase-list.js:102`（列表「去收货」按钮）
- `pages/purchase-detail/purchase-detail.js:33`（详情「去收货」按钮）
- `cloudfunctions/dataService/index.js:585`（首页「待收货」计数 `receivableStatuses`）

同时 `getPurchaseOrders/index.js:58-64` 的 `statusCounts` 和 `purchase-list.js:72-78` 的筛选 tab 都**不统计** `partial_received`——即使真有这个状态的单子，列表里筛不出来、首页也不计数。即它既不会被产出，也不会被统计，只有"可收货"这个判定在为它留口子。

**现在的实际出路只有一条：按实收填数，然后整单变异常。** `receivedQty = 0` 是被允许的（`createReceipt:198` 只拒绝 `< 0`），所以"今天来一半、明天补一半"只能这样走：

1. 本次 `receivedQty` 填实际到货数（或 0 表示拒收）
2. `createReceipt:239-241` 自动打 `isShortage`，无论勾没勾异常
3. 整单 `receipt_abnormal`，带价报表 ④ ⑥ 全部跳过
4. 叠加第 4 条：异常解决后带价账单也**没有任何代码路径能补出来**

即分批复货 = 必然变成异常单 + 必然走线下结算，中间没有"部分已收、剩余待收"的状态。

**另一个容易踩的点：拒收会产生两条异常记录。** `abnormal_record` 按"商品 × 异常类型"生成（`createReceipt:317-322`），所以拒收的一行同时是短收（实收 < 下单）和质量问题，会产生 2 条记录，各自独立走 `pending → processing → resolved → closed`。

### 若要支持分批验收，具体改动点

核心是把 `receipt` 从"一订单一单"改成"一订单多批"：

| 改动 | 位置 |
|---|---|
| `receipt` 增加 `batch_no`、`is_final`（是否最后一批） | 数据模型 |
| `receipt_item` 增加累计已收字段（当前只有 `received_qty` 与 `order_qty_snapshot`） | 数据模型 |
| 去掉"已存在收货单即拒"，改为"订单未收完可继续" | `createReceipt:220-222` |
| 明细校验从"覆盖全部订单行"改为"只覆盖本次要收的行"，并校验累计不超过下单量 | `createReceipt:212-214`、`:198` |
| 报表按批次生成，或按累计已收滚动重算 | `createReceipt:380-507` |
| `statusCounts` 与列表 tab 补上 `partial_received` | `getPurchaseOrders:58-64`、`purchase-list.js:72-78` |

**需要单独拍板的子问题：未收完的订单能不能出带价账单？** 建议能——按实收出账、剩余挂起，收齐后补一张。否则分批在资金上和现在一样走不通。

### 与前两条的关系

第 2 条（未审批可收货）、第 3 条（本条）、第 4 条（短收整单异常）**同一个根因**：当前模型假设"整单一次验收完"。所有需要"分批 / 部分 / 隔离"的业务都建立在推翻这个假设之上，三条建议一起拍板，不要分次。

---

## 4. 短收被强制判定为异常，正常部分也无法结算 —— `[阻塞]`

**待确认：短收订单是整单异常，还是"正常商品照常结算 + 短收部分单独跟进"？**

`createReceipt/index.js:239-241`：

```js
items.forEach(item => {
  if (item.receivedQty < item.orderQty) item.isShortage = true
})
```

只要**任一行**短收，整单收货状态即为 `abnormal`（订单状态 `receipt_abnormal`），并且：
- 异常单**不生成任何带价格报表**（`createReceipt/index.js:402-403`、`:478`），门店带价收货报表和供应商带价账单全部跳过；
- 异常明细行 `payableFlag` 强制为 `false`（`:266`），但整单不结算导致**连没有问题的其他商品也无法进入付款**。

对酒店场景（食材常有小差异）这可能是最常被投诉的点：**为了一根葱短收，整张单的正常货也结不了款。** 若业务希望"异常隔离到行级"，需要把报表拆成"可结算部分 + 异常部分"两份，而不是整单跳过。

---

## 5. 消息中心过滤 —— `[已定]` 跨门店已隔离；`[待确认]` 门店内是否全员可见

### 结论

- **跨门店泄漏不存在**：原记录说"厨师能看到其他门店的消息"，实测不成立。`getMessages` 从第一版起就同时按 `recipient_user_id` 和 `store_id` 过滤（HEAD 的 `dataService:273-276`），只是过滤发生在内存里、且取数方式有 bug。本轮把过滤下推到数据库。
- **本轮真正修的是另外两个**：消息静默丢失、未读数跨用户串号（见下表）。
- **仍需业务拍板**：厨师是否应该看到**本门店全员**的收货/异常消息——当前能看到。

### 现状（2026-09-11 工作区）

过滤条件下推到数据库，`dataService/index.js:388-402`：

```js
const recipientCondition = _.or([
  { recipient_user_id: '' },
  { recipient_user_id: userId },
  { recipient_user_id: _.exists(false) }
])
if (!GLOBAL_ROLES.includes(auth.user.role)) {   // chef / store_manager
  query = _.and([recipientCondition, _.or([
    { store_id: '' },
    { store_id: auth.user.default_store_id || '' },
    { store_id: _.exists(false) }
  ])])
}
```

- `GLOBAL_ROLES = ['super_admin', 'purchaser']`（`:8`）不受门店限制；厨师、店长限定在本门店。
- 过滤不会空转：所有写消息的地方 `store_id` 都是真值——`createPurchaseOrder:48`、`createReceipt:88`、`createReceipt:370`、`dataService.createMessage:178`。
- 未读改为**按用户**记录：`read_by` 数组（`:405-406`、`:422-426`），兼容旧的全局 `read` 布尔。厨师点已读不再清掉店长的未读。
- 首页 `pages/index/index.js:44` 只传 `action`，过滤完全由服务端按会话用户取；`utils/cloud.js:40-43` 统一注入 `authToken`，鉴权链路正常。

### 本轮修掉的两个真问题

| 问题 | 修复前（HEAD） | 修复后（工作区） |
|---|---|---|
| 门店消息静默丢失 | `:272` 先取**全局**最新 100 条再内存过滤，全公司消息超过 100 条时本门店的消息会掉出窗口，前端无任何提示 | 过滤条件下推到数据库（`:387` 注释亦写明） |
| 未读数串号 | `read` 是全局布尔（`:292`），厨师标记已读会让同门店店长的未读一起消失 | `read_by` 按用户记（`:405-406`、`:422-426`） |

### 遗留小口子（当前数据不会触发，但会咬人）

1. **`store_id: ''` = 全体可见，且没有强制**。现存所有写入方都填了真实门店，但 `createMessage`（`:177-178`）对 `recipientUserId` / `storeId` 的默认值就是 `''`——将来谁调它不传参数，就静默群发全公司（含厨师），不报错。
2. **空门店账号行为不一致**：其他读接口在 `default_store_id` 为空时直接返回 `-403 账号未关联有效门店`（`getPurchaseOrders:37`、`getReports:38`），`getMessages` 却静默降级成匹配 `store_id: ''`，等于把群发消息发给没绑店的账号。建议与其它接口统一。
3. **`read_by` 不去重**（`_.push(userId)`）。前端 `message.js:33` 挡了一次，服务端没挡，重复标记会持续拉长数组。

### 待确认

**厨师是否应该看到本门店全员的收货与异常消息？**

当前消息都是按门店广播、不按人定向（`recipient_user_id` 恒为 `''`），所以厨师会看到本门店店长的"收货验收完成 / 收货异常待处理"，内容含日期、门店名、异常类型。若异常处理责任在店长、不该落到厨师，应把这类消息改成 `recipient_user_id` 指向店长（`store_id` 仍保留，便于全员兜底查看），而不是按门店广播。

### 留痕（原记录，2026-09-11 首次排查）

原结论"厨师能在消息中心看到**其他门店**的收货验收完成 / 收货异常待处理标题和内容"**不准确**——`getMessages` 在 HEAD 就有 `recipient_user_id` + `store_id` 双重过滤，跨门店看不到。原判断把"前端 `pages/index/index.js:44` 不传角色和门店"读成了"服务端不过滤"，但过滤本来就在服务端、按会话用户取。真实的两个缺陷见上表。

---

## 6. 验收人身份以登录账号为准，前端传参无效 —— `[已定]`

`createReceipt/index.js:170`：

```js
receivedBy = user.name || user.username || receivedBy
```

收货单的"验收人"恒为当前登录账号姓名，客户端传入值被丢弃（这是防冒名，符合预期）。连带影响：**验收人只能是店长/管理员，厨师不能代验收、也不能在验收单上署自己的名**——与第 1 条是同一条业务决策的两面，若未来放开厨师验收，这里就是验收人字段。

---

## 7. 报表生成失败后无自动补偿 —— `[待确认]`

**待确认：收货已提交但报表缺失时，谁补、怎么补？**

`createReceipt/index.js:508-511`：交易已提交后，报表生成失败只把 `reportWarning` 回传给前端并记日志，**不会重试**。目前唯一的补偿逻辑是 `ensureReceiptMessage`（`:65-96`），只补**消息**，不补 `report_file` 记录。

影响：供应商带价账单缺失 → 采购对账时该笔供应商货款无凭据，且没有任何地方提示"这张单缺报表"。至少需要一处可查/可触发的补生成入口。

---

## 8. 提交后无法撤回，且驳回不会作废报表 —— `[阻塞]`

**待确认：已提交的采购单是否允许撤回？允许的话谁撤、撤到什么状态？**

### 现状一：系统里没有"撤回"这个动作

- `dataService/index.js:601-618` 的 action 列表没有 `cancelOrder`，全项目没有任何云函数做取消
- 状态字典 `utils/meta.js` 里没有 `cancelled` / `withdrawn` 之类状态
- 界面上唯一的"作废"是**报表**的 `superseded`（`report-list.wxml:37`、`report-history.wxml:31` 显示为「已作废」），指报表版本，不指订单

订单一旦提交即冻结：`createPurchaseOrder/index.js:123` 只允许 `draft` 编辑，提交后不能再改一行。唯一还能推动它的只有审核（`approved` / `rejected`）。

### 现状二：不存在"自动发给供应商"，也不存在任何推送通道

供应商**不是系统用户**——`authService/index.js:12-17` 的角色只有 `chef` / `store_manager` / `purchaser` / `super_admin`，全项目没有 `requestSubscribeMessage` 或任何通知代码。

所谓"发给供应商"的实际路径是：

1. 提交 → `createPurchaseOrder:298-341` 同步生成 ② 供应商订货汇总 CSV 并上传云存储
2. CSV 出现在**采购自己的**报表 tab
3. 采购手动下载，线下用微信 / 电话 / 邮件发给供应商

内部消息（`createPurchaseOrder:38-56`「采购申请已提交」）的 `recipient_user_id` 固定为空，只进本小程序消息中心，供应商看不到。

**时间窗口风险**：CSV 生成是同步的，提交按钮提示成功的那一刻 ② 已经在报表 tab 可下载。"能不能撤回"实际上完全取决于采购有没有在接下来几分钟内转发出去——系统不给缓冲。

### 现状三：驳回 ≠ 撤回，三处都会出问题

| 问题 | 位置 |
|---|---|
| `rejected` 是终态，不能改、不能重提、无作废路径 | 见第 3 条补充 |
| 驳回时 `event.items` 为空 → `qtyChanged = false`，**不触发报表作废** | `dataService/index.js:354-366` |
| 标记 `superseded` 的逻辑在 `regenerateApprovedOrderReports` 内部，只在"批准且改量"时被调用 | `dataService/index.js:224-226` |

结果：**驳回的一张单，它的 ① ② 报表在报表 tab 里仍然显示为正常有效**。供应商如果已经拿到那份 CSV，不会从系统里得到任何作废信号。

### 若要做撤回，改动点

| 改动 | 说明 |
|---|---|
| 状态字典加 `cancelled` | `utils/meta.js` |
| `dataService` 加 `cancelOrder` | 事务内改 `order_status`；沿用 `dataService:224-226` 模式把该单 ① ② 标 `superseded` |
| 前端加按钮 + 二次确认 + 原因 | 仅 `submitted`（未审核、未收货）状态可见 |
| 列表 tab / `statusCounts` 补上 | `purchase-list.js:72-78`、`getPurchaseOrders:58-64` |
| 发一条内部消息提醒采购去通知供应商 | `message` 集合 |

**必须和业务一起拍板的点**：系统标作废 ≠ 供应商撤销订单。CSV 若已线下发出，供应商那边仍需人工沟通。撤回功能必须配一条"需通知供应商"的强制确认，否则会出现"系统显示作废、供应商照旧送货"。

### 唯一的事前拦截手段：草稿

`purchase-create.js:273` 有 `saveDraft`，`purchase-detail.js:61` 有「提交审核」按钮；草稿不出报表、不发消息（`createPurchaseOrder:271-273` 直接 return），这个阶段随便改。目前这是唯一能避免"提交错了"的办法——**提交之后没有回头路**。

### 附带发现：订货报表里没有到货日期 → 已拆为独立条目

`delivery_date` 下单时必填校验（`createPurchaseOrder:101`）、详情页也显示（`purchase-detail.wxml:26`），但**从未写进任何一份发给供应商的报表**——供应商现在收到的文件里连"什么时候送到"都没有。此问题连同"报表缺单号/联系人"、"`related_date` 语义不统一"已拆为**第 9 条**。

---

## 9. 报表缺日期 / 单号 / 联系人，且 `related_date` 语义不统一 —— `[待确认]`

**待确认：报表模板应该带哪些字段？哪些是必填？**

改动本身不大（数据都是现成的），但字段口径需要采购和门店一起定，所以作为业务点记录。

### 现状一：6 份报表的 CSV 正文都没有日期列

| 报表 | 位置 | 实际列 |
|---|---|---|
| ① 门店下单报表 | `createPurchaseOrder:281` | 商品名称, 分类, 单位, 下单数量, 备注 |
| ② 供应商订货汇总 | `createPurchaseOrder:323` | 门店, 商品名称, 订货数量, 单位, 备注 |
| ③ 门店收货报表 | `createReceipt:383` | 商品名称, 下单数量, 实收数量, 单位, 验收状态, 异常类型, 备注, 是否可付款 |
| ④ 门店带价收货 | `createReceipt:406` | 商品名称, 实收数量, 单位, 单价, 小计, 是否可付款 |
| ⑤ 供应商到货汇总 | `createReceipt:456` | 商品名称, 门店, 到货数量, 下单数量, 单位, 验收状态, 异常类型, 备注 |
| ⑥ 供应商带价账单 | `createReceipt:484` | 商品名称, 门店, 到货数量, 单位, 单价, 小计, 可付款 |

审核改量后重出的 ①② 同样是这几列（`dataService:230`、`:264`）。

**缺的字段**：下单日期、期望到货、收货日期、采购单号、供应商联系人、门店地址。

### 现状二：日期只存在于三个地方，都不在 CSV 里

1. **文件路径 / 文件名** — `createPurchaseOrder:285`：`reports/store/${actualDate}/store-order-${storeName}-${actualDate}-${orderNo}-v${ver}.csv`，② 同理（`:328`）。订单号本身也含日期（`:207`）
2. **`report_file` 记录** — `:293` `related_date`、`generated_at`
3. **小程序界面** — `report-list.wxml:45` 显示日期、`:48` 显示「生成于」；`report-detail.wxml:11`「日期 · v1」、`:122`「生成时间」

**报表详情页的行数据里也没有日期** — `getReportDetail:67-80` 重建 rows 只有 `productName / category / unit / orderQty / isManual / remark`，wxml 表头只有「商品 / 分类 / 单位 / 数量」四列。

**后果**：供应商拿到的文件里连"什么时候送到"都没有。采购用微信转发通常还能保留文件名，但截图、或直接把表格内容复制发过去，**什么日期线索都没了**。采购单号同理，只在文件名里。

### 现状三：供应商信息库里有的字段，也没进报表

`dataService.saveSupplier` 存了 `contact_name` / `contact_phone` / `address` / `remark`，但 `createPurchaseOrder:311` 查供应商只取了名称：

```js
supplierMap[s.supplier_id].name = s.supplier_name
```

联系人和地址从来没写进任何报表。即采购发给供应商的订货单里，连"有事找谁、送到哪里"都没有。

### 现状四：`related_date` 一个字段两种含义

| 报表类型 | `related_date` 存的是 |
|---|---|
| ① ② 下单类 | **下单日期** `actualDate`（`createPurchaseOrder:293`） |
| ③ ④ ⑤ ⑥ 收货类 | **收货日期** `receiptDate`（`createReceipt:394`） |

报表 tab 的日期筛选（`report-list.wxml:21-27`、`getReports:60`）是精确匹配这个字段。对下单报表筛下单日、对收货报表筛收货日——**同一张单跨月下单、跨月收货时，会分别出现在两个月的筛选结果里**。按月对账容易混。

### 若要改，改动点

数据全是现成的，只是没写进去：

| 报表 | 建议补的列 |
|---|---|
| ① ② | 下单日期、期望到货、采购单号 |
| ③ ④ ⑤ ⑥ | 收货日期、采购单号 |
| ② ⑤ ⑥ | 供应商联系人、联系电话（可选） |

代码位置：6 个 CSV 生成处（`createPurchaseOrder:281`/`:323`、`dataService:230`/`:264`、`createReceipt:383`/`:406`/`:456`/`:484`）+ `getReportDetail` 的 rows 构建 + `report-detail.wxml` 的表头。

**另一个建议**：给 `report_file` 加 `basis_date_type` 字段（`order_date` / `receipt_date`），把 `related_date` 的语义固定下来。否则后面加日期区间查询（第 4 条提到的月度对账）会一直踩这个坑。

### 与其他条目的关系

- 第 4 条提到"报表只能按单日精确匹配，无区间查询"——加区间查询前必须先解决 `related_date` 语义不统一
- 第 8 条「订货报表里没有到货日期」是这一条的子集，已并入此处

---

## 10. 结算价格取"收货当天的现价"，下单不锁价 —— `[阻塞]`

**待确认：按下单日价还是收货日价结算？改价何时生效？价格口径含税/运费吗？**

### 现状

- 下单明细不存价格：`createPurchaseOrder/index.js:237-251` 的 item 字段没有 price。
- 结算价在验收时取数据库 `is_current = 1` 的现价（`createReceipt/index.js:232-247`，注释原话"价格以数据库中的当前供应商价格为准"）。
- 改价即时生效：`updateProductPrice/index.js:42-67` 事务内直接换 `is_current`；`effective_date` 只是记录字段——页面改价根本不传它（`pages/price-manage/price-manage.js:118-124` 的 `savePrice`），传了未来日期也立即生效。价格历史虽有 `effective_date` / `expiry_date`，取价路径只认 `is_current`，无法按日期还原"当时的价"。

### 待确认

1. 下单到收货间隔内涨价，差价落在门店还是供应商？（现实现 = 收货日价）
2. 改价要不要支持次日生效/预约生效？`effective_date` 现在是空壳。
3. 价格是否含税/含运费；单价输入无精度约束（可输 3.333，小计才保留 2 位分）。

---

## 11. 没配价的商品照常验收，账单里永远缺这一行 —— `[阻塞]`

**待确认：缺价时阻止验收、强制先配价，还是照收后提醒补价？补价后能否补账？**

### 现状

- 商品/供应商没维护价格时 `priceSnapshot = 0`（`createReceipt/index.js:247`），`payableFlag` 被强制 false（`:252`），该行以 0 元、"可付款 否"进入带价报表 ④⑥。
- 但整单状态仍是 `completed`（`:274`），**不产生异常记录、不产生任何提醒**——没人知道要补价。
- 改商品默认供应商同理：旧供应商的价格不随迁，新供应商没配价就收货，一样静默漏掉。

### 与第 7 条的关系

#7 是"报表生成了但失败丢失"，本条是"账本正常但该行天生不进账"。两件事都需要同一个"补生成/补价后补账"入口才能收口。

---

## 12. 审批环节三个口径没定 —— `[阻塞]`

**待确认：① 是否禁止自单自审？② 审核数量填 0 等于删行还是应拒绝？③ 审核要不要时限？**

### 现状

1. `purchaser` / `super_admin` 既能下单（`createPurchaseOrder/index.js:59`）也能审核（`dataService/index.js:284`），`auditOrder` 没有"自单自审"拦截。
2. 审核改量允许填 0（`dataService/index.js:319` 校验是 `qty >= 0`），即审批可逐行清零；全部清零的单照样 `approved` 进入待收货，收 0 也合法完成收货（`createReceipt/index.js:184` 只拒绝实收>下单）。
3. 审核无时效机制：`submitted` 单挂着没人审，无催办、无升级、无提醒；`rejected` 终态问题见第 8 条。

---

## 13. 手动商品（临时商品）没有任何结算路径 —— `[待确认]`

**待确认：手动商品是否就是"系统留痕、线下结算"的定位？要不要允许临时挂供应商/补录价格？**

现状：每单最多 5 个手动商品（`createPurchaseOrder/index.js:162-165`、`:183-186`），供应商强制为空。后果链：不进供应商订货汇总（`:302-303` 跳过 `unknown`）→ 收货时无价 `priceSnapshot = 0`（`createReceipt/index.js:247`）→ `payableFlag` false → ④⑥ 里 0 元"可付款 否"，也不产生异常记录。

---

## 14. 超收被硬性禁止 —— `[待确认]`

**待确认：允不允许超收并按实收付款？还是"绝不超收"就是业务规则？**

现状：实收>下单直接拒绝（`createReceipt/index.js:184`，前端 `pages/receive-verify/receive-verify.js:134-141` 同样拦）。供应商多送货时只能按下单数收、多出的货线下处理。

---

## 15. 异常处理的责任人与时效没定义 —— `[待确认]`

**待确认：异常由店长处理还是采购处理（要不要分类型）？closed 谁确认、业务上代表什么？要不要时效？验收要不要强制拍照？**

### 现状

- pending→processing→resolved→closed 的"开始处理/解决/关闭"三个动作权限完全一致（`dataService/index.js:590`、`:606`、`:640`），店长可以自己开自己关；无超时提醒。
- 处理结果只是一段文字，不回写订单、不触发补报表（与 #3/#4 的"异常解决后账单补不出来"叠加）。
- 验收照片非必填：前端允许 0 张（`receive-verify.js:105`），服务端只限最多 9 张——对账纠纷时没有留证要求。

---

## 16. 单据日期可回填任意过去日期 —— `[待确认]`

**待确认：允不允许补录过去的采购/收货？与 #9 的按日对账口径怎么衔接？**

现状：下单日期（`createPurchaseOrder/index.js:77-88`）与收货日期（`createReceipt/index.js:213-220`）以客户端传入为准，只校验格式，接受任意历史日期、无"补录"标记。正常页面只能选今天，但接口层补单是放行的。

---

## 17. 离职账号的处理口径 —— `[已定]` 软删除（停用即离职）

### 结论（2026-09-13 确认）

**离职 = 停用（`status: 0`），不做物理删除。** 业务上不区分"停用"和"离职"两种状态，离职就是一种带备注的停用。

理由：单据上可靠的身份锚点是 `user_id`（`created_by`），而收货人/审核人/异常经手人存的是**姓名字符串**（`createReceipt/index.js:156`、`dataService/index.js:338`、`:600`、`:624`）且姓名不唯一；物理删除会断掉"按人追溯"的关联线。

### 现状

- `deleteUser` 是物理删除（`authService/index.js:366-379` 直接 `.remove()`），删除后 username 被释放，新人可注册同名账号（`createUser` 重名检查只查现存记录 `authService/index.js:263-267`），历史责任人身份有混淆风险。
- 展示影响有限：`created_by_name` 是下单时快照（`createPurchaseOrder/index.js:206-208`），收货人/审核人也是姓名快照——丢的主要是 user_id → 人的追溯能力。

### 落地改动（2026-09-13 已实施）

| 改动 | 状态 | 位置 |
|---|---|---|
| 新增 `setUserStatus` action：停用（`status: 0`，同时清 session token）与启用；禁停自己和默认 `admin` 账号 | ✅ 已实施 | `authService/index.js:369`、路由 `:440` |
| user-manage 的「删除」按钮替换为「停用/启用」动态按钮，停用弹确认框明示"历史单据与记录保留" | ✅ 已实施 | `pages/user-manage/user-manage.js:198`、`user-manage.wxml:26` |
| `deleteUser` 保留但加前置校验：名下有未完结单据（`created_by` 按 user_id/_id/姓名三重匹配，状态含草稿/待审/待收）或 pending/processing 异常经手人时拒绝，提示改用停用 | ✅ 已实施 | `authService/index.js:396` |
| `deleted_at` 字段升级 | ❌ 未采用：停用账号保留在列表中展示（否则无法再启用），状态行已有「正常/停用」标识；重名检查本就查全部记录，软删记录占住 username 的效果不受影响 | — |
| 会话即时失效：`getSessionUser` 按 `status: 1` 过滤（`authService/index.js:95`）天然兜底，停用时额外清 token 防止重新启用后旧会话复活 | ✅ 已实施 | `authService/index.js:386-388` |
| 换店口径：`default_store_id` 改绑后历史单据留在原店（单据属于门店），已认可 | ✅ 无需改动 | — |

---

## 18. 草稿"店长可见但不可改" —— `[待确认]`

**待确认：草稿属于个人还是门店公共？店长要不要能代改/代提交厨师的草稿？**

现状：店长能看到本店所有人创建的草稿（`dataService/index.js:452-463` 对 store_manager 只按店过滤、不按人），但只有创建者本人能编辑/提交（`createPurchaseOrder/index.js:108-116`）。即厨师还没提交的采购内容店长全程可见。

---

## 19. 供应商报表"汇总"实际按单生成，不合并 —— `[待确认]`

**待确认：供应商侧想要按单的、还是按日/按周合并的订货单？**

现状：② 订货汇总、⑤ 到货汇总按"每张订单 × 每供应商"各生成一份（`createPurchaseOrder/index.js:302-325`），同一供应商同一天来 3 张单就收 3 个 CSV，不会合并。文件名叫"汇总"，实际粒度是"单店 × 单订单"。

---

## 附：两条边界确认（不单列条目，顺带问业务方）

- **系统边界**：现在只覆盖"下单→审批→收货→付款凭据"，完全没有库存/领用/成本核算——这个范围是不是终局定位。
- **容量口径**：商品列表 200 条（`getProducts/index.js:24`）、供应商 100 条（`getSuppliers/index.js:29`）、价格 200 条（`getProductPrices/index.js:23`）、用户 100 条（`authService/index.js:252`）、消息/异常各 100 条（`dataService/index.js:403`、`:563`）全部无分页截断；商品关键词搜索是取回 200 条后内存过滤（`getProducts/index.js:34-37`），超上限静默搜不到。商品/供应商预期规模决定要不要做真分页。

---

## 待办索引

| # | 问题 | 状态 | 影响 |
|---|---|---|---|
| 1 | 厨师验收资格 | `[已定]` 无资格 | — |
| 2 | 未审批订单可否收货 | `[阻塞]` 待拍板 | 审批流程被绕过 |
| 3 | 是否支持分批验收 | `[阻塞]` 待拍板 | `partial_received` 为空状态；分批复货必然变异常单 + 走线下结算 |
| 4 | 短收是否整单异常 | `[阻塞]` 待拍板 | 正常商品无法结算 |
| 5 | 消息中心过滤 | `[已定]` 跨门店已隔离（DB 端过滤 + 按用户未读）· `[待确认]` 厨师是否看本门店全员消息 | 门店内互通口径未定；`store_id:''` 无强制 |
| 6 | 验收人取登录账号 | `[已定]` | 与 #1 同源 |
| 7 | 报表缺失的补偿机制 | `[待确认]` | 对账缺凭据 |
| 8 | 提交后能否撤回 | `[阻塞]` 待拍板 | 无取消状态；驳回不作废报表，供应商拿到的 CSV 不会失效 |
| 9 | 报表该带哪些字段 | `[待确认]` | 6 份 CSV 都没有日期/单号/联系人；`related_date` 下单类存下单日、收货类存收货日 |
| 10 | 结算价格口径（下单价 vs 收货日现价、改价生效时机） | `[阻塞]` 待拍板 | 下单不锁价，结算取收货日现价；差价归属无约定；`effective_date` 空壳 |
| 11 | 缺价商品静默漏结算 | `[阻塞]` 待拍板 | 未配价商品照常收货但账单缺行，无异常记录、无提醒 |
| 12 | 审批环节口径（自审/数量清零/时效） | `[阻塞]` 待拍板 | 管理员可自单自审；审核数量可为 0；审核无催办 |
| 13 | 手动商品结算口径 | `[待确认]` | 无供应商无价格，不进订货报表、不进付款，纯线下 |
| 14 | 是否允许超收 | `[待确认]` | 实收>下单被硬性拒绝，多送的货只能线下处理 |
| 15 | 异常处理责任人与时效 | `[待确认]` | 三动作权限一致可自开自关；resolved/closed 含义未定义；照片非必填 |
| 16 | 单据日期补录 | `[待确认]` | 下单/收货日期接口层可回填任意过去日期 |
| 17 | 离职账号处理 | `[已定]` 软删除（停用即离职）· 2026-09-13 已实施 | 前端改停用/启用、服务端 `setUserStatus` 新增、`deleteUser` 加未完结单据守卫 |
| 18 | 草稿可见性 | `[待确认]` | 店长可见全店草稿但不可改 |
| 19 | 供应商报表汇总粒度 | `[待确认]` | ②⑤ 按单生成不合并，"汇总"名不副实 |

> **建议：#2 / #3 / #4 一起拍板。** 三条的根因是同一条假设——「整单一次验收完」。当前模型下：审批可以先被收货绕过（#2）、分批做不到（#3）、短收会连累整单结算（#4）。分次决策很容易先放掉 #3 再回来纠结 #4，然后发现 #4 的解法（异常隔离到行级）其实已经隐含了"可以只收一部分"。详见 #3 末尾「与前两条的关系」。
>
> **建议：#10 / #11 最先拍板**——直接决定钱对不对得上；**#12** 关乎审批公信力；**#13 / #14** 与验收模型一族（#2/#3/#4）相关，可一并拍板。

---

*创建日期：2026-09-11 · 2026-09-13 增补 #10–#19（其中 #17 离职账号口径已拍板：软删除）。新增条目请延续上面的格式：先写现状 + 代码位置，再写待确认问题。*
