# 业务模糊点确认清单

> **用途**：记录当前小程序里"代码行为已定、但业务口径还没跟需求方对齐"的地方。每条都写明【现状】（代码实际怎么做的，附文件位置）和【待确认】（需要业务方拍板的问题）。
>
> **状态标记**：`[已定]` 结论明确无需再讨论 · `[待确认]` 需要业务方答复 · `[阻塞]` 影响上线或资金结算
>
> **维护约定**：新增一条时先写现状（带代码位置），再写待确认问题。已拍板的把结论写回【现状】并标记 `[已定]`，不要直接删掉，留痕便于追溯。
>
> **文档结构**：第一部分为通用业务模糊点（#1–#9）；第二部分为**供货商（supplier 角色）业务模糊点**（S1–S8），单独成章便于业务方按角色分开拍板。

---

# 第一部分：通用业务模糊点

## 0. 角色对照表

| 角色 key | 界面显示名 | 是否绑定门店 | 说明 |
|---|---|---|---|
| `chef` | 门店下单人员 | 必须 | 下单人。登录页角色名写作「下单人员」 |
| `store_manager` | 店长 | 必须 | 门店负责人 |
| `purchaser` | 管理员 | 否（可跨店） | 采购/集团管理员 |
| `super_admin` | 超级管理员 | 否（可跨店） | 唯一，`admin` 账号不可删除、不可改角色 |
| `supplier` | 供货商 | 否（绑定供货商档案） | 供货商门户账号，模糊点见第二部分 S1–S8 |

代码位置：`cloudfunctions/authService/index.js:12-21`

---

## 1. 厨师（下单人员）是否具有验收资格 —— `[已定]`

### 结论：**没有**

验收提交角色白名单只包含 `store_manager` / `purchaser` / `super_admin`，`chef` 不在其中。

### 现状（三处都在拦）

1. **后端硬拦截**：`cloudfunctions/createReceipt/index.js:102`
   ```js
   if (!['store_manager', 'super_admin', 'purchaser'].includes(user.role)) {
     return { code: -403, msg: '当前账号无权提交收货验收' }
   }
   ```
2. **前端不显示验收入口**：`pages/purchase-list/purchase-list.js:97`、`pages/purchase-detail/purchase-detail.js:33`
   ```js
   const canReceiveRole = currentUser.role !== 'chef'
   ```
3. **厨师看不到收货记录**：`cloudfunctions/getReceipts/index.js:34-36` 对 `chef` 直接返回空列表。

### 厨师实际权限范围

| 能力 | 是否可用 |
|---|---|
| 创建采购单 | ✅ |
| 查看自己创建的订单 | ✅（`getPurchaseOrderDetail/index.js:47` 校验 `created_by` 为本人） |
| 查看门店范围报表 | ✅（`getPurchaseOrderDetail/index.js:78` 只放行 `report_scope === 'store'`） |
| 收货验收 / 查看收货记录 | ❌ |
| 采购审核、商品/供应商/价格管理 | ❌（`pages/index/index.js:93`，仅 `purchaser` + `super_admin`） |
| 查看消息中心 | ✅ 已按门店隔离，但**不按人定向**——能看到本门店全员的消息（见第 5 条） |

### 遗留问题

- ~~验收页 `pages/receive-verify/` 自身没有角色前置校验~~ **已修复（2026-09-19 核验）**：`receive-verify.js:47-55` 的 `onLoad` 已加 chef 拦截（toast 后 `navigateBack`），取用户方式与其他页面统一（`app.globalData.userInfo`）。
- `pages/receive-list/` 当前是**无入口死页面**：`app.json` tabBar 和首页均无收货入口，验收入口只在采购列表/详情页的「去收货」按钮上；该页 `receive-list.js:19` 仍保留 `chef` 的 `createdBy` 过滤分支，属废代码。

---

## 2. 未审批的订单可以直接验收 —— `[已定]`（2026-09-19 核验已实现）

### 结论：**必须审批通过后才能收货**（B2，采用原方案 A）

### 现状

`createReceipt/index.js:166`（事务外）与 `:313`（事务内）两处状态白名单均为：

```js
if (!['approved', 'report_generated', 'partial_received', 'to_receive'].includes(order.order_status)) {
```

`submitted` 已不在白名单中，未审批订单无法收货。前端按钮口径与后端一致（`purchase-list.js:103`、`purchase-detail.js:33`、`receive-list.js:37`）。

### 留痕（原记录，2026-09-11 排查）

原状态白名单含 `submitted`，店长可在审批前直接验收，审批流被事实性绕过。现已按"审批是收货前置"修正。

---

## 3. "部分收货"目前做不出来 —— `[已定]`（2026-09-19 核验已实现）

### 结论：**允许分批收货，同一订单可多张收货单，全部收齐才完结**（B3）

### 现状

"一单一票、整单一次验收完"的模型已推翻：

- `createReceipt/index.js:195-200` 按订单行聚合历史累计实收（`historyQtyMap`），`:215-217` 校验「本次实收 + 历史累计 ≤ 下单量」；原"明细必须覆盖全部订单行"（原 `:212-214`）与"已有收货单即拒"（原 `:220-222`）已删除，改为事务内仅拦截 `received` 状态（`:307-312`）。
- `receipt` 增加 `batch_no`（`:236-241`、`:326`）与 `is_final`（`:296-302`、`:327`）。
- `partial_received` 已落地为本批未收齐时的订单状态（`:391-394`），本批收齐则 `received`（有异常 `receipt_abnormal`）。
- 短收判定改为按「历史累计 + 本次」与下单量比较（`:258-264`），未收完的行不算少货。
- 首页「待收货」计数已含 `partial_received`（`dataService getOrderStats:597`）。

### 仍存在的缺口（未修完）

- `getPurchaseOrders/index.js:58-73` 的 `statusCounts` **不统计** `partial_received`，`purchase-list.js:72-79` 的筛选 tab 也**没有"部分收货"**入口——部分收货的单子在列表里筛不出来（只能靠"去收货"按钮识别，按钮已含该状态，`purchase-list.js:103`）。
- 拒收仍会产生两条异常记录：`abnormal_record` 按"商品 × 异常类型"生成（`createReceipt:355-388`），拒收一行同时是短收 + 质量问题，2 条记录各自独立走 `pending → processing → resolved → closed`。

### 留痕（原记录，2026-09-11 排查）

原实现中 `partial_received` 是空状态：既无代码路径产出，列表/首页也不统计，分批复货只能整单变异常走线下结算。详见下方"补充（2026-09-11 二次排查）"的原始分析。

### 补充（2026-09-11 二次排查）

**空状态的影响面比原记录更大。** 除了 `createReceipt` 的两处校验，`partial_received` 还被 3 处读作"可收货"的判定条件：

- `createReceipt/index.js:166`（事务外状态校验）
- `createReceipt/index.js:277`（事务内二次校验）
- `pages/purchase-list/purchase-list.js:102`（列表「去收货」按钮）
- `pages/purchase-detail/purchase-detail.js:33`（详情「去收货」按钮）
- `cloudfunctions/dataService/index.js:585`（首页「待收货」计数 `receivableStatuses`）

同时 `getPurchaseOrders/index.js:58-64` 的 `statusCounts` 和 `purchase-list.js:72-78` 的筛选 tab 都**不统计** `partial_received`——即使真有这个状态的单子，列表里筛不出来、首页也不计数。即它既不会被产出，也不会被统计，只有"可收货"这个判定在为它留口子。

**现在的实际出路只有一条：按实收填数，然后整单变异常。** `receivedQty = 0` 是被允许的（`createReceipt:198` 只拒绝 `< 0`），所以"今天来一半、明天补一半"只能这样走：

1. 本次 `receivedQty` 填实际到货数（或 0 表示拒收）
2. `createReceipt:239-241` 自动打 `isShortage`，无论勾没勾异常
3. 整单 `receipt_abnormal`，带价报表 ④ ⑥ 全部跳过
4. 叠加第 4 条：异常解决后带价账单也**没有任何代码路径能补出来**

即分批复货 = 必然变成异常单 + 必然走线下结算，中间没有"部分已收、剩余待收"的状态。

**另一个容易踩的点：拒收会产生两条异常记录。** `abnormal_record` 按"商品 × 异常类型"生成（`createReceipt:317-322`），所以拒收的一行同时是短收（实收 < 下单）和质量问题，会产生 2 条记录，各自独立走 `pending → processing → resolved → closed`。

### 若要支持分批验收，具体改动点

核心是把 `receipt` 从"一订单一单"改成"一订单多批"：

| 改动 | 位置 |
|---|---|
| `receipt` 增加 `batch_no`、`is_final`（是否最后一批） | 数据模型 |
| `receipt_item` 增加累计已收字段（当前只有 `received_qty` 与 `order_qty_snapshot`） | 数据模型 |
| 去掉"已存在收货单即拒"，改为"订单未收完可继续" | `createReceipt:220-222` |
| 明细校验从"覆盖全部订单行"改为"只覆盖本次要收的行"，并校验累计不超过下单量 | `createReceipt:212-214`、`:198` |
| 报表按批次生成，或按累计已收滚动重算 | `createReceipt:380-507` |
| `statusCounts` 与列表 tab 补上 `partial_received` | `getPurchaseOrders:58-64`、`purchase-list.js:72-78` |

**需要单独拍板的子问题：未收完的订单能不能出带价账单？** 建议能——按实收出账、剩余挂起，收齐后补一张。否则分批在资金上和现在一样走不通。

### 与前两条的关系（历史注，2026-09-19 已关闭）

第 2 条（未审批可收货）、第 3 条（本条）、第 4 条（短收整单异常）曾是**同一个根因**：旧模型假设"整单一次验收完"。该假设现已被推翻，三条均按 B2/B3/B4/B6 推荐方案实现完毕，合并拍板事项关闭。

---

## 4. 短收被强制判定为异常，正常部分也无法结算 —— `[已定]`（2026-09-19 核验已实现）

### 结论：**行级异常隔离——正常商品照常结算，异常部分单独跟进**（B4/B6）

### 现状

- 短收按「历史累计 + 本次实收」与下单量比较，仅标记异常行（`createReceipt/index.js:258-264`），未收完的行不算少货。
- 付款裁决按行（`:288-293`）：纯少货（shortage）行**按实收数量付款**（账单以 `received_qty` 计价，未到货部分自然不出现）；质量/错货行不进付款结算。
- 带价报表（门店 ④ `:456-489`、供应商 ⑥ `:538-545`）**始终生成**，只含 `payableFlag=true` 的可付款行；异常行的数量与类型记录在不含价的收货报表中。不再"整单跳过"。
- 异常处理完成后可补结算：`dataService.settleReceipt`（`:616`）在异常全部 resolved/closed 后由管理员生成补充带价账单（`report_id` 带 `_S` 后缀防重复）；`resolveAbnormal`（`:515`）支持 `pay_received`/`reject` 付款裁决。

### 留痕（原记录，2026-09-11 排查）

原实现任一行短收即整单 `receipt_abnormal`，带价报表 ④ ⑥ 全部跳过，正常商品也结不了款（"为了一根葱短收，整张单的正常货也结不了款"）。已改为行级隔离。

---

## 5. 消息中心过滤 —— `[已定]` 跨门店已隔离；`[已定]`（2026-09-19）异常消息定向店长，正常消息保留广播

### 结论

- **跨门店泄漏不存在**：原记录说"厨师能看到其他门店的消息"，实测不成立。`getMessages` 从第一版起就同时按 `recipient_user_id` 和 `store_id` 过滤（HEAD 的 `dataService:273-276`），只是过滤发生在内存里、且取数方式有 bug。本轮把过滤下推到数据库。
- **门店内可见口径（2026-09-19 拍板）**：**异常类消息定向给店长**（处理责任人，与 B1 口径一致），**正常收货完成类消息保留门店广播**（厨师等全员可见到货信息）。已在 `createReceipt` 两处消息写入落地（事务内 `:415-421`、补写 `ensureReceiptMessage` `:93-95`，经 `getStoreManagerId` 查店长 `user_id`，查不到回退广播）。
- **本轮修掉的另外两个**：消息静默丢失、未读数跨用户串号（见下表）。

### 现状（2026-09-19 核验）

过滤条件下推到数据库，`dataService/index.js:393-407`：

```js
const recipientCondition = _.or([
  { recipient_user_id: '' },
  { recipient_user_id: userId },
  { recipient_user_id: _.exists(false) }
])
if (!GLOBAL_ROLES.includes(auth.user.role)) {   // chef / store_manager
  query = _.and([recipientCondition, _.or([
    { store_id: '' },
    { store_id: auth.user.default_store_id || '' },
    { store_id: _.exists(false) }
  ])])
}
```

- `GLOBAL_ROLES = ['super_admin', 'purchaser']`（`:8`）不受门店限制；厨师、店长限定在本门店。
- 过滤不会空转：所有写消息的地方 `store_id` 都是真值——`createPurchaseOrder:48`、`createReceipt:88`、`createReceipt:370`、`dataService.createMessage:178`。
- 未读改为**按用户**记录：`read_by` 数组（`dataService:410-412`、`:431-433`），兼容旧的全局 `read` 布尔。厨师点已读不再清掉店长的未读。
- 首页 `pages/index/index.js:44` 只传 `action`，过滤完全由服务端按会话用户取；`utils/cloud.js:40-43` 统一注入 `authToken`，鉴权链路正常。

### 本轮修掉的两个真问题

| 问题 | 修复前（HEAD） | 修复后（工作区） |
|---|---|---|
| 门店消息静默丢失 | `:272` 先取**全局**最新 100 条再内存过滤，全公司消息超过 100 条时本门店的消息会掉出窗口，前端无任何提示 | 过滤条件下推到数据库（`:387` 注释亦写明） |
| 未读数串号 | `read` 是全局布尔（`:292`），厨师标记已读会让同门店店长的未读一起消失 | `read_by` 按用户记（`:405-406`、`:422-426`） |

### 遗留小口子（当前数据不会触发，但会咬人）

1. **`store_id: ''` = 全体可见，且没有强制**。现存所有写入方都填了真实门店，但 `createMessage`（`dataService:176-178`，2026-09-19 核验仍存在）对 `recipientUserId` / `storeId` 的默认值就是 `''`——将来谁调它不传参数，就静默群发全公司（含厨师），不报错。且 `createMessage` 未初始化 `read_by: []`，新消息缺该字段。
2. **空门店账号行为不一致**：其他读接口在 `default_store_id` 为空时直接返回 `-403 账号未关联有效门店`（`getPurchaseOrders:37`、`getReports:38`），`getMessages` 却静默降级成匹配 `store_id: ''`，等于把群发消息发给没绑店的账号。建议与其它接口统一。
3. **`read_by` 不去重**（`markMessageRead`/`markAllRead` 仍 `_.push(userId)`，`dataService:431`、`:444`，2026-09-19 核验未修）。前端 `message.js:33` 挡了一次，服务端没挡，重复标记会持续拉长数组。

### 待确认

**厨师是否应该看到本门店全员的收货与异常消息？**

当前消息都是按门店广播、不按人定向（`recipient_user_id` 恒为 `''`），所以厨师会看到本门店店长的"收货验收完成 / 收货异常待处理"，内容含日期、门店名、异常类型。若异常处理责任在店长、不该落到厨师，应把这类消息改成 `recipient_user_id` 指向店长（`store_id` 仍保留，便于全员兜底查看），而不是按门店广播。

### 留痕（原记录，2026-09-11 首次排查）

原结论"厨师能在消息中心看到**其他门店**的收货验收完成 / 收货异常待处理标题和内容"**不准确**——`getMessages` 在 HEAD 就有 `recipient_user_id` + `store_id` 双重过滤，跨门店看不到。原判断把"前端 `pages/index/index.js:44` 不传角色和门店"读成了"服务端不过滤"，但过滤本来就在服务端、按会话用户取。真实的两个缺陷见上表。

---

## 6. 验收人身份以登录账号为准，前端传参无效 —— `[已定]`

`createReceipt/index.js:170`：

```js
receivedBy = user.name || user.username || receivedBy
```

收货单的"验收人"恒为当前登录账号姓名，客户端传入值被丢弃（这是防冒名，符合预期）。连带影响：**验收人只能是店长/管理员，厨师不能代验收、也不能在验收单上署自己的名**——与第 1 条是同一条业务决策的两面，若未来放开厨师验收，这里就是验收人字段。

---

## 7. 报表生成失败后无自动补偿 —— `[已定]`（2026-09-19：缺报表标记 + 管理员手动补生成）

### 结论：**发现靠系统标记，补偿靠管理员手动触发；不做自动重试**

### 现状

- **发现**：`createReceipt` 报表生成失败时（`:591-620`），给订单打 `missing_reports: true`、给收货单打同标记（列表随记录带出），并发管理员定向消息「收货报表生成失败，请补生成」。缺口不再静默。
- **补生成**：`dataService.regenerateReceiptReports`（仅 `purchaser`/`super_admin`），按 `receipt_id` 重读 `receipt_item`（价格快照在库），重走 ③④⑤⑥ 四类报表（`report_id` 带 `_RG` 后缀），成功后清除缺报表标记。前端入口：收货记录页对缺报表单显示「缺报表」标签 + 管理员可见「补生成」按钮（`receive-list.wxml:43-49`）。
- **不做自动重试的理由**：失败多为云存储/网络问题，事务外自动重放有版本号竞争与重复报表风险；人工触发幂等且量极少。
- 异常导致的账单缺失仍走 `settleReceipt` 补结算；审核改量走 `regenerateApprovedOrderReports` 重算。三条补偿路径各司其职。

### 留痕（原记录，2026-09-11 排查）

供应商带价账单缺失 → 采购对账时该笔供应商货款无凭据，且全项目无任何补偿入口。现已有完整"标记 → 通知 → 补生成"链路。

---

## 8. 提交后无法撤回，且驳回不会作废报表 —— `[已定]`（2026-09-19 核验已实现）

### 结论：**审批前可作废；审批后仅管理员作废（须无收货记录）；店长/采购员可申请取消**（B8）

### 现状

- **作废已实现**：`dataService.cancelOrder`（`:734-780`，action 注册于 `:838`）。仅管理员（`GLOBAL_ROLES`）可操作，必须填原因；允许状态 `submitted / approved / report_generated`（`:752`），已有收货记录的（`partial_received / to_receive / received / receipt_abnormal`）拒绝作废、走异常流程（`:749-751`）；事务内把该单 ① ② 报表标 `superseded`（`:766-769`），并发内部消息提醒通知供应商（`:772-778`）。
- **取消申请已实现**：`dataService.requestCancel`（`:783-818`）——任意角色可对 `submitted / approved / report_generated / partial_received / to_receive` 的订单提交申请（写 `cancel_requested` 字段 + 消息），管理员确认后执行 `cancelOrder`。
- **前端入口**：`purchase-detail.js:40-96`——`canCancel`（`submitted` 且采购员/超管）、`canRequestCancel`（非 chef 且未申请过）、作废有"请先线下通知供应商"强制确认（`:82`）；列表有"已作废" tab（`purchase-list.js:78`），`statusCounts` 已含 `cancelled`（`getPurchaseOrders:58-73`）。
- **B7 驳回复制新草稿已实现**：`purchase-detail.js:38-39`（仅 `rejected` 显示）+ `copyToDraft`（`:104-137`），复用 `createPurchaseOrder` 的 draft 模式，原单保留；前端按钮 `purchase-detail.wxml:65`。
- 状态字典 `utils/meta.js:18` 已有 `cancelled`（已作废）。

### 仍存在的口径问题（不变）

- **系统标作废 ≠ 供应商撤销订单**：CSV 若已线下发出，供应商那边仍需人工沟通，系统只发内部消息提醒。
- **驳回时订单本无报表可作废**：`rejected` 只发生在 `submitted` 阶段，此时订单尚未生成 ③④⑤⑥；且驳回的单现在有"复制为新草稿"出路，不再是死终态。

### 留痕（原记录，2026-09-11 排查）

当时无任何取消动作、无 `cancelled` 状态、驳回是死终态且 ① ② 报表不会失效。上述功能均为其后迭代补齐。

### 供应商接单与报表发送（B9 口径，未变）

供应商仍**不是系统用户**（`authService:12-17` 仅四角色，无推送代码）。"发给供应商"的路径：提交时同步生成 ② 供应商订货汇总 CSV（`createPurchaseOrder:298-341`）→ 采购从报表 tab 下载 → 线下微信/电话/邮件发送。与图中 B9 推荐方案（第一阶段人工发送）一致。

---

## 9. 报表缺日期 / 单号 / 联系人，且 `related_date` 语义不统一 —— `[已定]`（2026-09-19 核验：字段已补，语义问题仍在）

### 结论：**6 份 CSV 均已补单号 / 日期 / 批次 / 联系人信息头**（B10）

### 现状

- ① ② 下单类报表信息头含：采购单号、门店、下单日期、期望到货、经办人，② 另含供应商联系人（`createPurchaseOrder:281`、`:327`；审核改量重发 `dataService:237-241`、`:276-280`）。
- ③④⑤⑥ 收货类报表信息头含：采购单号、门店、收货日期、下单日期、期望到货、验收人、批次（`createReceipt:419-422`；补结算账单 `dataService:694`）。
- **联系人只在 ②（订货汇总）里**，收货类报表信息头无联系人字段——符合"联系人属供应商订货单"口径，如需收货单也带联系人再提。
- `related_date` 语义仍是一个字段两种含义：下单类存下单日（`createPurchaseOrder:293`）、收货类存收货日（`createReceipt:448` 附近）、汇总类存传入日期（`generateSummaryReport:199`，月报也存具体日）。**`basis_date_type` 字段未加**（全库无此字段，2026-09-19 核验），跨月对账混筛的风险保留。

### B11 日/月汇总报表（已实现，有一处缺口）

- `generateSummaryReport/index.js` 完整支持 `daily` / `monthly`（`:138`），角色限制 `store_manager / purchaser / super_admin`（`:134`），店长强制本店（`:142-145`），写入 `store_daily_summary_report` / `store_monthly_summary_report`（`:198-215`）。
- 前端入口：`report-list.js:123-137`「生成今日日汇总 / 生成本月月汇总」。
- **缺口（2026-09-19 核验）**：`getReports/index.js:52-55` 的类型白名单与 `utils/meta.js:22-29` 的 `reportTypeMap` 均**未纳入**两个 summary 类型——报表能生成入库，但列表按类型筛选时会被排除、且无图标/文案标签。

### B12 多设备登录（已实现）

`authService/index.js:179-183`：每台设备一条会话（`sessions` 数组），只保留未过期会话、上限 5 条挤出最旧；登出仅删当前设备会话（`:208-221`）；改密后清空全部会话（`:244`、`:361`、`:389`）。账号不共用（角色白名单控制），个人账号可按需多设备登录。

### 留痕（原记录，2026-09-11 排查）

当时 6 份 CSV 正文无任何日期/单号/联系人列，这些信息只存在于文件名。现已全部补入信息头。

---

# 第二部分：供货商（supplier 角色）业务模糊点（S1–S8 已于 2026-09-20 全部拍板并实施）

> 供货商角色链路（d2161f5）：登录（authService）→ 门户（supplier-home）→ 订单确认/发货（confirmSupplierOrder / getSupplierOrders）→ 收货对账（getSupplierReceipts）→ 协议价查询（getProductPrices / updateProductPrice）。以下为该链路上代码行为已定、但业务口径未拍板的点。

### S9 手动商品订货/结算双断链 —— `[待确认]`（2026-09-21 新增）

**现状**：建单时手填的"手动商品"（每单限 5 个，`supplier_id` 为空、无协议价）在全链路有三处断点：

1. **订货单不含它**：② 供应商订货汇总 CSV 按 `supplier_id` 分组生成，空值归入 `unknown` 后被过滤（`createPurchaseOrder` 报表段 `filter(sid => sid !== 'unknown')`；审核改量重发 `regenerateApprovedOrderReports:303` 同样过滤）——**没有任何供应商在订货单里看到手动商品**，也无新订单下推通知（`notifySuppliersNewOrder` 按 `supplier_id` 分组，空值跳过）。
2. **结算进不去**：收货取价按 `supplier_id + product_id` 查协议价表，手动商品查不到 → `price_snapshot` 恒为 0（`createReceipt:333-336`）→ 按口径"0 价行不可付款"（`payable_flag=false`），④⑥ 带价报表与 `settleReceipt` 补结算均被 `price_snapshot > 0` 过滤掉。且手动商品无供应商归属，**没有补救入口**（价格表挂不上去）。
3. **实际流程全在线下**：现状是门店线下微信/电话告知散货商，收货时只填实收数量，金额闭环缺失，系统内无付款凭据。

**待确认**：

1. 手动商品是否应强制挂供应商 + 填单价（走完整链路：进 ② 订货单 → 供应商收通知/确认/发货 → 按快照价结算）？
2. 应急场景确实无供应商的（市场零售/代买），是否允许留空但标记"线下采购"，收货时补录单价即可付款（不参与订货推送，只补结算口）？
3. 手填单价是否需要软约束（如超该分类均价 X 倍时审核页高亮提醒）？

**推荐口径（待业务方确认）**：**允许混合下单**——同一张采购单可同时含档案商品与手动商品，按行隔离（供应商只见自己的行，含挂了供应商的手动行），不强制拆"手动商品专用单"（拆单不增加隔离性，反而使审批/通知/报表全链路新增单据类型分叉）。手动商品建单时必填供应商 + 单价（快照直接存明细，不查价格表；档案商品仍强制走协议价表，不开放手改价防绕穿 S5 口径）；无供应商的应急商品允许留空标"线下采购"，收货补录单价进结算。

---

### S1 "已发货"标记窗口极窄 —— `[已定]（2026-09-20 已实施）`

**拍板结论（2026-09-20）**：动作级白名单：确认接单仍限 submitted/approved，标记发货放宽到 report_generated/to_receive（confirmSupplierOrder 已按 action 区分校验）。

**现状**：供货商只能对 `submitted` / `approved` 状态的订单确认接单或标记发货（`confirmSupplierOrder/index.js:10`）。但按 B2"先审批后收货"，订单审核后流转到 `report_generated` / `to_receive`，此时供货商**无法再标记发货**。确认接单窗口与内部审批是并跑的，"标记发货"在真实流程里几乎没有可用时机。

**待确认**：审核通过后（`to_receive`）是否允许供货商补标发货？还是发货标记仅作参考、不需要系统跟踪？

### S2 改量/取消后供货商确认状态不重置 —— `[已定]（2026-09-20 已实施）`

**拍板结论（2026-09-20）**：审核改量重发报表时清除该单所有 supplier_confirmations（打回待确认），并向订单创建人发内部消息提醒线下通知供应商重新确认；作废订单不清理确认记录（dataService.regenerateApprovedOrderReports 已实现）。

**现状**：
- 管理员审核改量会重发 ② 供应商订货 CSV（`dataService regenerateApprovedOrderReports:256-315`），但**不重置** `supplier_confirmations`——供货商之前"已确认"的是改量前的数量口径。
- `cancelOrder` 作废订单也不清确认记录，只发内部消息（`:772-778`）。

**待确认**：改量后是否应把该单所有供货商确认状态打回 `pending` 并要求重新确认？作废时确认记录是否需要清理（当前单已 `cancelled`，`getSupplierOrders` 会显示为"已作废"tab，影响有限）？

**补充（2026-09-21）**：修复误清确认——`regenerateApprovedOrderReports`（`dataService:244`）新增 `qtyChanged` 参数（默认 `true`），确认重置仅在改量时执行；缺报表补生成入口 `regenerateOrderReports`（`:818`）传 `false`，补生成不再误清供货商确认、不再误发"订单改量，供货商需重新确认"消息。

### S3 供货商无任何系统内消息触达 —— `[已定]（2026-09-20 已实施）`

**拍板结论（2026-09-20）**：维持 B9 线下人工触达，不加系统内消息通道；cancelOrder 作废消息文案已改为提醒采购经办人线下告知供应商。

**现状**：`getMessages` 按门店过滤（`dataService:393-407`），supplier 不绑门店、看不到任何消息；门户（supplier-home）也没有消息入口。作废订单时"提醒通知供应商"实际是**发给内部管理员的消息**（`cancelOrder:772-778`），供货商在系统内完全无感知。B9 口径是第一阶段人工线下发送。

**待确认**：供应商触达是否维持"采购线下微信/电话/邮件"（B9）？若是，代码里"通知供应商"的注释/文案应改为"通知采购经办人"，避免误导；若否，需给 supplier 角色加消息通道（`getMessages` 增加按 `scope_id=supplier_id` 的过滤分支）。

### S4 补充结算账单供货商不可见 —— `[已定]（2026-09-20 已实施）`

**拍板结论（2026-09-20）**：不放开 getReports，对账仍走线下；供货商对账信息由 S7 的异常裁决结果展示兜底。

**现状**：`settleReceipt` 生成的补结算带价账单（`_S` 后缀）只进 `report_file`，而 `getReports:64-66` 对 `supplier` 角色直接返回 -403 无权查看报表。供货商"收货记录"页能看到 `payable_flag=false` 或数量不符的异常行标红（`supplier-receipts.js:50`），但异常解决后的**最终结算口径（补结算金额）系统内无法对账**。

**待确认**：是否放开供货商查看自己的 ⑥ 供应商带价账单（含 `_S` 补结算）？还是对账永远走线下？

### S5 协议价"未来生效日期"立即生效 —— `[已定]（2026-09-20 已实施）`

**拍板结论（2026-09-20）**：不支持预约调价，后端仅允许当天生效日期，未来/过去日期一律拒绝（updateProductPrice 已加拦截）；effective_date 字段保留。

**现状**：`updateProductPrice:69-94` 支持传 `effectiveDate`，但新价插入即刻 `is_current: 1`、旧价立刻置 0——填未来日期也会**马上生效**。收货计价用的是收货单上的价格快照，所以实际只影响供货商门户/价格管理页的展示口径。

**待确认**：未来生效日是否应到点才切换 `is_current`（查询逻辑改为 `effective_date <= today` 取最新一条）？还是约定只能填当天、后端直接拒绝未来日期？

### S6 供货商确认状态对内部不可见 —— `[已定]（2026-09-20 已实施）`

**拍板结论（2026-09-20）**：订单详情页按明细行展示供货商确认标签（已确认/已发货/待确认），不做超时提醒；核验发现 purchase-detail.js/wxml 此前已实现，本轮确认无需改动。

**补充（2026-09-21）**：「待确认」兜底标签已补齐——此前 pending 是派生默认值、从不写入 `supplier_confirmations`，详情页无记录时不挂标签，"待确认"表现为无标签。现 `purchase-detail.js` 的 `confirmTagOf`（`:38-42`）对无记录的供货商派生 `pending` 标签；非 chef 角色商品清单改为**按供货商分组展示**（组头：供应商名 + 确认标签，`supplierGroups`，wxml/wxss 已配套），chef 维持平铺且后端本就不下发供应商名（`getPurchaseOrderDetail:86-96`）；`draft/rejected/cancelled` 不打标签，`received/receipt_abnormal/completed` 记 `done` 与供货商端口径一致。已知小瑕疵：供应商档案被删时组名兜底显示内部 supplierId（极端场景，未处理）。

**现状**：`supplier_confirmations[supplier_id]` 只有供货商自己读写（`confirmSupplierOrder`），管理员端（purchase-list / purchase-detail）不展示"该供应商是否已确认接单"，也没有超时未确认提醒。

**待确认**：采购/管理员是否需要看到各供应商确认进度（详情页加标签、超时提醒）？

### S7 拒收与异常处理进度供货商不可查 —— `[已定]（2026-09-20 已实施）`

**拍板结论（2026-09-20）**：供货商收货记录页只读展示每行异常的类型/处理状态/处理结果/付款裁决；getSupplierReceipts 已 join abnormal_record，supplier-receipts 前端已展示（未结案红色、已结案灰色）。

**现状**：`received_qty=0` 的拒收行在供货商收货记录里显示金额 ¥0 并标红，但异常记录（`abnormal_record`）的处理状态（`pending → processing → resolved → closed`）与裁决结果（`pay_received` 补款 / `reject` 拒赔）供货商不可见。

**待确认**：拒收与质量异常的处理进度/裁决结果是否需要供货商侧可查？

### S8 `partial_received` 被归为"已完成" —— `[已定]（2026-09-20 已实施）`

**拍板结论（2026-09-20）**：partial_received 从 DONE_ORDER_STATUS 移出，保留供货商已有确认状态（不降回待确认），收齐后才归为已完成（getSupplierOrders 已改）。

**现状**：`getSupplierOrders:13` 把 `partial_received` 归入 `DONE_ORDER_STATUS`，供货商视角显示为"已完成"（`deriveConfirmStatus:49`）。但按 B3 分批收货，此时剩余批次可能还没到货，供货商不应停止备货/发货。

**待确认**：`partial_received` 是否应保留为"进行中"（允许继续确认/标记发货），收齐后才归为 `done`？

**补充（2026-09-21）**：`confirmSupplierOrder` 的 `SHIPPABLE_ORDER_STATUS` 已加入 `partial_received`（`:12`）——部分收货中订单供货商可继续补标发货（确认接单白名单不变），剩余批次未到前供货商不被状态卡死。

---

## 待办索引（第一部分：通用业务）

| # | 问题 | 状态 | 影响 |
|---|---|---|---|
| 1 | 厨师验收资格 | `[已定]` 无资格 | 前端 `receive-verify` 页面已补角色前置校验 |
| 2 | 未审批订单可否收货 | `[已定]` 必须审批通过后才能收货（B2） | `createReceipt:166/313` 白名单已无 `submitted` |
| 3 | 是否支持分批验收 | `[已定]` 允许分批，收齐才完结（B3） | `receipt` 加 `batch_no`/`is_final`，`partial_received` 已落地；⚠️ 列表 `statusCounts` 与筛选 tab 未纳入该状态（见 #3 缺口） |
| 4 | 短收是否整单异常 | `[已定]` 行级隔离：正常部分照常结算（B4/B6） | 纯少货按实收付款；质量/错货行不进付款，走异常流程 |
| 5 | 消息中心过滤 | `[已定]` 跨门店已隔离（DB 端过滤 + 按用户未读）· `[已定]` 异常消息定向店长、正常消息门店广播（2026-09-19） | `createReceipt` 消息写入经 `getStoreManagerId` 定向；`store_id:''` 无强制的小口子仍在 |
| 6 | 验收人取登录账号 | `[已定]` | 与 #1 同源 |
| 7 | 报表缺失的补偿机制 | `[已定]` 缺报表标记 + 管理员手动补生成，不做自动重试（2026-09-19） | `createReceipt` 打 `missing_reports` + 定向通知；`dataService.regenerateReceiptReports` 补生成 |
| 8 | 提交后能否撤回 | `[已定]` 审批前可作废；审批后仅管理员作废且须无收货记录；店长/采购员可申请取消（B8） | `cancelOrder`/`requestCancel` 已实现，关联报表标 `superseded` |
| 9 | 报表该带哪些字段 | `[已定]` 6 份 CSV 均已补单号/日期/批次/联系人信息头（B10） | `related_date` 语义未加 `basis_date_type` 字段（低优先级） |

## 待办索引（第二部分：供货商业务）

| # | 问题 | 状态 | 影响 |
|---|---|---|---|
| S1 | 供货商"已发货"标记窗口极窄 | `[已定] 已实施（2026-09-20）` | `confirmSupplierOrder` 仅 `submitted`/`approved` 可操作，审批后无法补标发货 |
| S2 | 改量/取消后供货商确认状态不重置 | `[已定] 已实施（2026-09-20）`·补生成误清已修（2026-09-21） | 改量重发 CSV 重置 `supplier_confirmations`；缺报表补生成不再误清确认（`qtyChanged` 参数） |
| S3 | 供货商无系统内消息触达 | `[已定] 维持线下触达（2026-09-20）` | `getMessages` 不含 supplier 分支，"通知供应商"实为内部消息，触达走线下 |
| S4 | 补结算账单供货商不可见 | `[已定] 对账走线下，由 S7 兜底（2026-09-20）` | `getReports` 对 supplier 返回 -403，异常后最终结算口径系统内无法对账 |
| S5 | 协议价未来生效日期立即生效 | `[已定] 已实施（2026-09-20）` | `updateProductPrice` 填未来日期也即刻 `is_current:1` |
| S6 | 供货商确认状态对内部不可见 | `[已定] 已实现（2026-09-20 核验）`·待确认兜底标签已补（2026-09-21） | 详情页按供货商分组展示确认标签，无记录派生「待确认」；chef 平铺不见供应商名 |
| S7 | 拒收/异常处理进度供货商不可查 | `[已定] 已实施（2026-09-20）` | `abnormal_record` 状态与裁决结果供货商不可见 |
| S8 | `partial_received` 被归为"已完成" | `[已定] 已实施（2026-09-20）`·补标发货已放宽（2026-09-21） | `partial_received` 不算完成、保留确认状态；已加入发货白名单可补标发货 |
| S9 | 手动商品订货/结算双断链 | `[待确认]`（2026-09-21 新增） | 手动商品不进 ② 订货单、无下推通知，`price_snapshot` 恒 0 不可付款，金额闭环全在线下 |

## 已实现的对齐项（2026-09-19 核验）

- **B2 先审批后收货 / B3 分批收货 / B4·B6 行级结算隔离**：见 #2、#3、#4 条目现状。
- **B7 驳回复制新草稿**：`purchase-detail` 详情页对 `rejected` 单显示「复制为新草稿」（`copyToDraft`，`:104-137`），复用 `createPurchaseOrder` draft 模式，原单保留。
- **B8 取消/作废**：`cancelOrder` + `requestCancel` 两阶段（管理员直接作废 / 店长采购员申请取消），关联报表标 `superseded`。
- **B11 日/月汇总报表**：`generateSummaryReport` 云函数支持 `daily`/`monthly`，报表页有「生成今日日汇总 / 生成本月月汇总」入口。
- **B12 多设备登录**：`authService:179-183` 每设备一条会话、最多保留 5 条（挤出最旧），个人账号可多设备按需登录，账号不共用（角色白名单控制）。
- **B5 补结算**：`dataService.settleReceipt` 在异常全部 resolved/closed 后由管理员生成补充带价账单（`report_id` 带 `_S` 后缀防重复）；`resolveAbnormal` 支持 `pay_received`/`reject` 付款裁决。

## 遗留缺口汇总（2026-09-19 核验，均为小改动）

| 缺口 | 位置 | 说明 |
|---|---|---|
| ~~列表筛不出部分收货单~~ 已修复（2026-09-19） | `getPurchaseOrders`、`purchase-list.js` | `statusCounts` 与筛选 tab 已纳入 `partial_received`（「部分收货」tab） |
| ~~summary 报表被类型筛选排除~~ 已修复（2026-09-19） | `getReports`、`utils/meta.js` | 白名单已纳入 `store_daily/monthly_summary_report`，`reportTypeMap` 已补标签图标 |
| ~~`related_date` 语义未固定~~ 已修复（2026-09-19） | `report_file` 数据模型 | 12 处写入点均已补 `basis_date_type`（`order_date`/`receipt_date`/`summary_date`），历史数据无此字段需按类型兜底 |
| ~~`createMessage` 默认值群发风险~~ 部分修复（2026-09-19） | `dataService:176-178` | `read_by: []` 已补；`recipientUserId`/`storeId` 默认 `''` 保留（现有调用方均传真值，作为兜底广播语义） |
| ~~`read_by` 不去重~~ 已修复（2026-09-19） | `markMessageRead`/`markAllMessagesRead` | 已按 `read_by` 是否含该用户判断后再 push |
| 门店内消息可见口径 | ~~业务决策~~ 已拍板（2026-09-19） | 异常消息定向店长、正常消息广播，已落地（见 #5）；「异常已解决」消息同口径定向店长 |
| 报表生成失败无补偿 | ~~catch~~ 已闭环（2026-09-19） | 收货报表：`missing_reports` 标记 + `regenerateReceiptReports`（见 #7）；订货报表 ①②：`createPurchaseOrder` 失败同样打标 + `regenerateOrderReports` 补生成 |

> **历史注**：#2/#3/#4 曾建议"一起拍板"（根因同为「整单一次验收完」假设）。该假设已被推翻，三条均已按 B2/B3/B4/B6 推荐方案实现，拍板事项关闭。

---

*创建日期：2026-09-11。新增条目请延续上面的格式：先写现状 + 代码位置，再写待确认问题。*
